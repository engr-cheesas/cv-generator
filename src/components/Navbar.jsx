// import { useState } from "react";

const Navbar = () => {
    // const [isOpen, setIsOpen] = useState(false);
    return (  
        <nav className="bg-electric-blue text-white p-4 fixed top-0 w-full h-15">
            <div className="text-lg font-bold"> EZCV </div>
        </nav>
    );
}

export default Navbar;